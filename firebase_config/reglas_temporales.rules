// 🚨 REGLAS TEMPORALES PARA TESTING - CAMBIAR DESPUÉS

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORAL: Permite todo por 30 días para testing
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2025, 2, 10);
    }
  }
}

// 📝 NOTA: Después cambiar por las reglas de producción en firestore_rules.rules
